[pytest]
addopts = --strict-markers --import-mode=importlib
testpaths = .
markers =
    keyboard: requires keyboard casting bindings and sustained DevBench input
    startup(name): requires a named configuration loaded at game startup
